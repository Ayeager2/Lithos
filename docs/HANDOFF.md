# Handoff — Lithos

AI-optimized continuity doc. Bullets not prose. Drop content when shipped; never grow into paragraphs.

---

## Project
**Lithos** — React 19 + Vite long-arc incremental game with cosmic horror flavor. Browser-only (eventually Steam). 8 eras planned: Scavenger → Awakening → Settler → Awakened World → Arcane Industry → Eldritch Reckoning → Ascendant → Cosmic. Hidden alignment, prestige, ascension. **Currently Era 3 feature-complete.**

## Doc map
Read in order:

1. `docs/AI_CONTEXT.md` — file map · state shape · rules · where-to-add-what tables (start here, ~5 min)
2. `docs/HANDOFF.md` — this file: current state + next moves
3. `docs/systems.md` — per-system status (🟢/🟡/⬜/🔮). Open only when working on that system.
4. `docs/ERA_PLAN.md` — design rationale for cross-era arcs
5. `docs/BUGS.md` — known paper-cuts
6. `docs/roadmap.md`, `docs/architecture.md` — design history. Skip unless you need rationale.

## Architectural rules
- **Content-as-data.** `src/content/*.js` plain objects. No functions inside content.
- **Thin reducer + pure systems.** `src/state/reducer.js` dispatches → `src/systems/*` runs logic.
- **Persistent vs run split.** `state.persistent` survives prestige; `state.run` wipes. Revealed resources auto-persist via `persistent.permanentlyKnown` — any resource with a `hiddenUntil` rule, once revealed before a prestige, stays revealed forever.
- **Pure scene composition.** `systems/scene.js` decides what shows; UI just renders.
- **Hidden alignment + World Score never shown as a number.** Surface through consequences.
- **Anti-spam from day one.** Cooldowns; no key-repeat auto-fire.
- **Accessibility-first.** Reduced motion, dyslexia fonts, no flashing without opt-in.

## State (current commit)

### Recent progress (#138–#236)

- **#232-#236 Era 5 polish pass** — Five punch-list items shipped. (1) **Ritual combat (#232)** — Communion Heralds now have a real obsession-meter loop: `engageHerald` initializes meter at 50% of max + rounds counter; per-round `ritualAttack(mode)` lets the player push the meter UP (claim) or DOWN (push back). Herald reacts with 50% chance to nudge it back. Win at meter=max (claim) or meter=0 (push back) within rounds; lose if rounds exhausted. (2) **Real combat (#233)** — Defiance Heralds now run a full HP-based boss fight via `heraldAttack`. Reads equipped weapon for damage range, applies Herald defense, then Herald counter-attacks with their accuracy + damage range. Win at heraldHp=0 → reward + Swordplay XP. Lose at playerHp=0 → drain + close out (player not killed, just dropped to 1 HP). (3) **Touched Memory prestige (#234)** — PRESTIGE reducer case now reads `persistent.echoUpgrades.touchedMemory.level` and preserves [1, 3, 10, -1] of `void_residue` + `firstLightShard` per tier. Cosmic Memory always preserved. Carried via `persistent.reckoningCarryPending`; `seedAscensionRun` folds it into the new run's inventory. (4) **Apex Fire CTA (#235)** — TownView now shows a path-colored apex banner when `reckoningPhase === "apex"` with cost line + Fire button gated by `canFireApex`. Player no longer needs dev panel. (5) **Clock-slow from path buildings (#236)** — `tickReckoning` now reads each built building's `effect.clockSlowMult`; when path-buildings (Sky Anchor / Black Garden / Bastion of Stone, each 0.75) exist, the apex timestamp pushes forward by (1-mult) × elapsed each tick. Three slow buildings stack multiplicatively for a max ~42% slowdown. Smoke test verified: Sky Anchor alone pushes apex 15s forward over 1 min elapsed. End-to-end ritual + combat + clock-slow + prestige all green.

- **#225-#231 Era 5 implementation (Eldritch Reckoning) landed end-to-end** — Era 5 is now feature-complete with content + systems + UI. Headline pieces: (1) **Entry** — `era5Eligible` returns true when Era 4 + (worldScore≥90 OR alignment.evil≥25 OR apexSummonBound). SYNC_ERA fires "The Sky Bends" event (sanity −15) + calls `startReckoning(run)` which picks an arc + starts the clock. (2) **Reckoning clock** — single `RECKONING_DURATION_MS` config (default 3h) in `src/content/reckoning.js`. New `src/systems/reckoning.js` owns the tick. Three phases (shudders / heralds / apex) at 33/33/33 fractions. Pauses while tab closed (only counts online time via `reckoningStartedAt`). Reducer ticks every TICK; TownView banner shows arc + phase + remaining + paused state. (3) **Three Heralds** — `src/content/heralds.js`: Mouth at the Gate (33%), Shape of What You Built (50%), The Listener (66%). Each has **three path-flavored shapes** — Mending = dialog (3 alignment-keyed choices each), Communion = ritual combat (obsession meter), Defiance = straight combat. `tickReckoning` spawns them at their fractions; TownView Herald banner renders the right UI per kind. Surviving grants Void Residue / First-Light Shard / fragments. The Listener applies a 0.5 sanity + 0.5 morale/min drain for the rest of the era after survival (via `tickListenerDrain`). (4) **5 Era 5 buildings** — Sky Anchor (Mending, drains worldScore, slows clock), Black Garden (Communion, +evil/min), Bastion of Stone (Defiance, +20 def), Echo Sanctum (any-path safe room +1 sanity/min +3 housing), Conduit Spire (apex-enabling +6 spirit/min). All path-buildings tag a `clockSlowMult` field for future clock-slowing implementation. (5) **3 resources** — Void Residue, First-Light Shard, Cosmic Memory (persists across prestige). (6) **3 apex events** — `src/systems/apex.js`: The First Light Returns (Mending), The Embrace (Communion — burns 15 villagers, zeroes worldScore, locks evil), The Last Stand (Defiance — 30% population loss). All grant +1 Cosmic Memory + stamp `apex:<arc>` etching. FIRE_APEX action + dispatcher. (7) **Touched Memory echo upgrade** — 4 tiers (8/25/75/150 echoes) in `content/echoes.js`. Designed to preserve 1/3/10/all Era 5 resources across prestige. (Wiring into save/prestige flow lives in #233 followup.) (8) **Path-switch penalty stacks all four** — 20 min off clock + burn 30 aether iron + 5 conduit cores + 50 fragments + -20 sanity + opposite-arc counter zeroed + Heralds re-spawn. `applyPathSwitchPenalty(state, newArc)` in reckoning.js; PATH_SWITCH action. (9) **2 new skills** — Reckoning Lore + Cosmic Bargaining (arcane category). Each Herald resolution grants +20 / +15 XP regardless of outcome. (10) **Dev panel scrub controls** — 17 buttons in DevPanel Era 5 section: force entry, ±5m/±30m/±1h clock adjust, 30m/1h/3h duration presets, pause/resume, spawn each Herald, live stats readout (clock remaining + arc + phase + Heralds spawned/survived). Critical for playtest sweet-spot tuning. **Verified end-to-end via smoke test**: Era 5 entry works, clock fires, Heralds spawn, resolution grants XP + drops, apex fires + locks phase + grants Cosmic Memory.

- **#216-#221 Era 4 UI polish pass** — All six pending UI items wired. (1) **TownView banners** — red rebellion banner when `rebellionActiveSince` is set (with suppression indicator when an evil summon / high evil alignment is holding the line); pre-rebellion morale-low warning with countdown to trigger; tainted-buildings list with per-building morale drain summary; active summon chip showing icon + arc-color + remaining time. (2) **Cleanse-taint flow** — new `🦠 Tainted` section in TownView shows each tainted building with a cleanse button (5 fragments at Stone Altar). New CLEANSE_TAINT action + `performCleanseTaint(state, buildingId)` helper + store dispatcher. (3) **DevPanel Era 4 section** — 13 new buttons: 🔮 Force Era 4 entry, 📦 Era 4 stockpile, 🔥 Force rebellion, 🌾 End rebellion, 🦠 Taint random building, plus one Bind button per summon (8 total). Live stats row shows era / rebellion status / tainted count / active summon id. (4) **Armor in crafting** — armor pieces tagged with `discipline` field (warrior+ranger → blacksmithing/tailoring, mage → tailoring). `getVisibleTools` extended to include all 45 armor pieces filtered by era + building gates + research. EquipmentInventoryGrid already had the armor branch — pieces now slot into head/chest/leggings/boots/gloves via the Slot component. Set bonuses fire when all 5 of a class+tier are equipped (read via `getActiveSetBonus`). (5) **Tinker tab in Crafting** — new "🪛 Tinker" discipline tab. `getVisibleTools` filters tinker items by player's tinker skill level (hard-gated). Items show their synergy skills + cost. Renamed `craftCost` → `cost` so the standard crafting flow picks them up. (6) **Summons section in CharacterView** — 🪐 Summons tab in the side nav. Lists all 8 summons split visually by arc (evil = burgundy border, good = green border). Each card shows description + bonuses + bind cost + a Bind button gated by `canBindSummon`. Live active-summon card at top with expiry timestamp + Dismiss button. Gated on Summoning Circle (shows hint to build it if missing).

- **Bitdefender truncation diagnosed + worked around** — Real-time scanning on `C:\Users\AnnaN\Documents\Lithos` was locking files mid-write during Edit tool calls, leaving partial content on disk (was misdiagnosed as OneDrive sync — that turned out to be a separate stale registry hint). Workaround that worked: atomic-write via `tempfile.mkstemp` + `os.replace` (single-inode rename, no streaming window for the scanner to interrupt). Permanent fix: add Lithos folder to Bitdefender exclusions (Antivirus → Manage Exceptions). After exclusion, normal Edit tool calls work again.

- **#205-#215 Era 4 implementation landed in one pass** — All ten Era 4 chunks shipped end-to-end. The headline pieces: (1) **Entry trigger + The Hum** — `computeEra` now returns 4 when Era 3 + any of: 25 villagers + Temple + Stone Altar / 3+ Arcane Studies paths mastered / worldScore ≥ 60 / alignment.evil ≥ 10. SYNC_ERA stamps `settlement:era:4` etching + writes `run.era`. Story event fires sanity −8 + the narration ("The forges are too quiet…"). (2) **Buildings + resources** — 8 new buildings (Aether Foundry → aether iron; Conduit Array +3 spirit/min −0.5 worldScore/min; Automaton Bay no-consumption labor; Echo Mill ration crafting −0.1 morale/min; Council Hall +25% pop growth +5 housing; University Wing ×1.20 study speed; Iron Bastion +10 def 0.20× raid mult; Sigil Wards −15% raid freq −20% sweep) plus Summoning Circle. 3 new resources (Aether Iron, Ration, Conduit Core). `requires.era` gate added to canBuild. (3) **Era 4 companions** — Tin Automaton (no consumption + ×1.10 production + 2 def), Cult Initiate (spirit/sanity +0.5/min, morale −1, rune +5%), Tame Demon (6 def, 10% weapon drop, 15% patrol sanity tax, +1 evil on recruit). Companion bonus consumer in `tickConsumption` reduces effective population for the automaton; `tickRecipeProduction` reads productionMult; patrol.js applies the demon sanity tax. (4) **Aether weapons** — 5 in `content/weapons.js`: Aether Sword/Greatsword/Crossbow/Staff/Warhammer. All gate on aetherFoundry. Each has +1-2 enchant slots over iron. (5) **Class armor** — new `src/content/armor.js`: 45 pieces (Warrior/Ranger/Mage × bronze/iron/aether × head/chest/leggings/boots/gloves) generated procedurally from a stat budget. Set bonus when all 5 of a class+tier are equipped. Wired into `getDefense` and `getStatCombatBonuses`. (6) **Tinker skill + 9 items** — new `src/content/tinker.js` with synergy-weighted recipes (effective skill = tinker×1.0 + Σ(synergySkill×weight)). Items: Smoke Bomb, Trip Wire, Caltrop Bag, Acid Vial, Flash Charge, Spring Snare, Aether Grenade, Web Spinner, Recall Beacon. Skills file gains Tinker + Sigilcraft + Summoning + stubs for butchering + magicCombat. (7) **Summoning** — new `src/systems/summoning.js` + `src/content/summons.js` with 8 summons split evil-arc (Skitter-Form, Shade-Walker, Cinder Hound, Wraith of the Hollow) vs good-arc (Whisper-Bound, Garden-Spirit, Forgehand, Aspect of the First Light). New `run.activeSummon` slot stacks with companion. Bind via BIND_SUMMON action; summon bonuses fold into morale tick, passive sanity/spirit, defense, world score, production multiplier (Garden-Spirit) or single-building x1.5 (Forgehand). Apex summons gate on Voidcall/Light path mastery + 4+ skills at level 8. Etchings: summon:first / summon:apex:first / summon:<id>:first. (8) **Rebellion** — new `src/systems/rebellion.js`. Tracks `moraleLowSince` when morale crosses below 20; after 5 sustained minutes stamps `rebellionActiveSince` + alert banner. `tickRebellion` fires per-60s damage rounds (70% resource sweep 30% / 40% building damage / 25% villager loss 1-2 / 10% assignment clear). Resolution: morale ≥ 30 (natural), evil summon (suppresses ticks, doesn't clear), alignment.evil ≥ 20 (fear suppression), good summon raising morale, or Aspect on bind (clears immediately). Stamps `settlement:rebellion:first` etching. (9) **Era 4 threats** — 3 new events in `content/events.js`: Tainted Outriders (0.95× sweep + 1 building dmg), Quiet Census (choice: refuse → −3 sanity + good / let count → −2 pop + −5 sanity + evil), Something Lives in the Walls (taintBuilding effect). New `effects.taintBuilding` handler in `systems/events.js`. Tainted buildings drain 0.3 morale/min each via the morale tick. `performCleanseTaint(state, buildingId)` in town.js cleanses for 5 fragments at Stone Altar. (10) **Dev panel helpers + state** — devStockEra4 (aether/cores/rations), devForceRebellion + devEndRebellion, devTaintBuilding, devBindSummon, devForceEra4. New run state fields: `activeSummon`, `moraleLowSince`, `rebellionActiveSince`, `lastRebellionTickAt`, `taintedBuildings`, `era`, `_era4PathsCount`. TICK reducer now ticks summoning + rebellion every cycle. **Pending UI polish** (not in this pass): TownView banner for active rebellion, summon binding UI, tinker item crafting tab, armor slots in CharacterView, dev panel button wiring.

- **#Era4-doc Era 4 design locked (revised v4)** — `docs/era4_design.md` now ~275 lines, locked through #215 phasing. Key adds since v3: (1) **3 Era 4 companions** gated on Council Hall (Tin Automaton — no consumption + ×1.10 production, Cult Initiate — spirit/sanity/rune up + morale drain, Tame Demon — defense + weapon drop + sanity risk + alignment evil). (2) **5 Aether-tier weapons** (#209). (3) **Class armor** — Mage/Ranger/Warrior across bronze/iron/aether = 45 pieces across Era 2-4; aether tier adds +1 enchant slot per piece. (4) **Tinker skill + 9 items** — synergy-driven (Tinker × 1.0 + Σ synergySkill × weight) using Alchemy, Hunting, Blacksmithing, Survival, Runesmithing, Sigilcraft. ONLY tinker can craft + use. Items: Smoke Bomb, Trip Wire, Caltrop Bag, Acid Vial, Flash Charge, Spring Snare, Aether Grenade, Web Spinner, Recall Beacon. (5) **Summoning skill + Summoning Circle building + 8 summons split by alignment arc**. Evil arc enforcers (Skitter-Form, Shade-Walker, Cinder Hound, Wraith of the Hollow) suppress rebellion ticks via fear/horror, often drain morale further. Good arc supporters (Whisper-Bound, Garden-Spirit, Forgehand, Aspect of the First Light) raise morale, boost production, and break active rebellions. Apex summons (Wraith / Aspect) mutually exclusive — opposite-path gating. (6) **Rebellion mechanic** — when `morale < 20` for 5 sustained minutes, `run.rebellionActiveSince` stamps + rebellion banner shows + `tickRebellion` fires per-60s ROUND with damage rolls (70% resource sweep 30%, 40% building damage, 25% villager loss, 10% assignment clear). Resolution paths: morale climb back ≥30 / evil summon suppression / high alignment evil suppression / good summon morale boost / Aspect immediate clear. Phasing locked at #205-#215 with rebellion as #213. **OneDrive footgun**: edits to design docs in OneDrive folders truncate mid-sync — the fix is atomic-write via Python tempfile + os.replace into the same directory. Used this pattern this session; file held at 275 lines.

- **#204 Pre-Era-4 companion polish** — Three small fixes. (1) Storage cards in TownView now show per-resource cap totals with the Stable Mule multiplier baked in: `Caps: +57 food · +23 bread · +17 bird_meat (×1.15 via companion)` when Stable Mule is active. (2) Active companion chip lands on TownView header between population and prestige banner: shows icon + name + "walks with you" + description tooltip. Green-tinted so it reads as a positive. (3) Fixed phantom weapon ID in companion weaponDropChance — was `"club"`, now `"woodenClub"` (the actual weapon ID in content/weapons.js). Log line also de-camelCases the ID so it reads as "Wooden Club" not "woodenClub".

- **#203 Companion bonuses wired into consumer systems** — All five bonus types now flow through the canonical readers: (1) `storage.js getResourceCap` reads `storageCapMult` → Stable Mule's ×1.15 actually expands inventory caps; (2) `passive.js applyPassiveProduction` reads `spiritPerMin` and the NEW `hpRegenPerMin` accumulator → Spirit Familiar drips spirit and Stray Dog/Old Veteran heal HP between actions; (3) `gathering.js performGather` reads `gatherChanceBonus` (adds to skillGatherBonus) and `gatherDropMult` (multiplies the drop qty alongside yieldMult) → Pet Crow and Stable Mule actually boost gather output; (4) `patrol.js rollDrops` reads `runeChanceBonus` and applies it only to rune resources (any id ending in "Rune"), plus a new post-victory `weaponDropChance` roll that grants a random Era 1-2 weapon (club/boneKnife/stoneAxe) → Old Veteran's 10% weapon drop now actually fires.

- **#202 Companion / pet system** — Five companions across Era 1-3 in new `src/content/companions.js`. Era 1: Stray Dog (🐕 +1 HP regen/min + 5% gather success; gated on wildDog kill), Pet Crow (🐦‍⬛ ×1.1 gather drops + 3% rune chance). Era 2: Old Veteran (🪖 +4 defense + 10% weapon drop + 1 HP regen; gated on Walls), Stable Mule (🐴 ×1.15 storage cap + ×1.05 gather; gated on Marketplace). Era 3: Spirit Familiar (🦋 +0.3 spirit/min + 5% rune chance + 1 HP regen; gated on Stone Altar + arcaneAwakening). New `systems/companions.js` with `canRecruit`/`performRecruit`/`setActiveCompanion`/`getActiveCompanionBonus`/`getOwnedCompanions`. One active at a time. Recruit consumes one-time cost; active companion bonuses flow through `getActiveCompanionBonus(state)` for any system that wants them. Defense.js now folds companion defense into getDefense (so Old Veteran contributes to raid sweep math + army strength). New `run.companions = { active, owned }`. New actions: RECRUIT_COMPANION + SET_ACTIVE_COMPANION + store dispatchers. New 🐾 Companions section on CharacterView between Equipment and Altar — shows all companions filtered by era+1 (so the player sees the next-era one as preview), each card has bonuses list, cost row, and Recruit / Activate / Dismiss button based on state. Jump-nav gains 🐾 Companions. Dev panel: grant each of the 5 companions instantly.

- **#201 Small-batch polish** — Six pieces shipped together. (1) **3 NPC caravan events**: Wandering Tinker (Era 1, wood↔hide, stone↔sinew), Salt Caravan (Era 2, coin↔salt, iron↔coin), Pilgrim Caravan (Era 3, coin/frag↔fragments, coin+frag↔Splinter Rune). All are choice events. (2) **Prestige hint banner** on TownView: shows when `getPrestigeReward(state).echoes >= 5` with reasons in tooltip. (3) **Worker reassignment pulse**: CSS keyframe `townAssignPulse` fires when the staffing span remounts via `key={assigned}`. (4) **Net delta hover breakdown**: new `getNetProductionBreakdown(state)` returns per-resource per-source contributions; TownView Net row tooltip lists every building + consumption line. (5) **Era 4 design doc** at `docs/era4_design.md` — 200-line sketch: cosmic-industrial premise, entry triggers, 8-building roster (Aether Foundry, Echo Mill, Council Hall, Iron Bastion, Conduit Array, Automaton Bay, University tier-2, Sigil Wards), new resources (channeled steel, ration, conduit core), threat evolution, phasing. (6) **Save migration smoke test extended** at `scripts/smoke_migrate.test.mjs` covering all new state fields (#170-#199); also verifies the system helpers don't throw on a migrated old save. Passes 16/16 checks + 10 function smoke calls.

- **#200 Polish pass on #196-#199** — Morale tooltip now lists each contributing factor with deltas (Base +50, Temple +15, Cottage +5, Active shortage -20, etc.) plus current vs target. Header chip shows "→ drifting to N" when current != target. New `getMoraleFactors(state)` export returns the breakdown for the UI. Trade route countdown on Marketplace card: "🪙 Next trade in 2m 13s" — updates live with the 1s TownView refresh. Dev panel: Set Morale 0/50/100, Force trade route fire, Give trade stock (100 wood + 100 stone + 20 hide), Stamp sample settlement etchings. Live morale counter row.

- **#199 Settlement morale** — New run.morale (0-100, default 50). New tickMorale runs every 30s, drifts toward an equilibrium computed by getMoraleEquilibrium: +15 Temple, +10 Moot Hall, +5 Cottage, +5 Ale, +5 Bread, +5 sanity>75 (-15 if <25), -20 any active shortage, -10 no shelter at all. New getMoraleMult: 0.5× at morale 0 → 1.0× at 50 → 1.25× at 100, linear. Applied to tickRecipeProduction so production scales with town happiness. New TownView morale row colored by tier (Despondent/Grim/Settled/Hopeful/Joyful).
- **#198 Settlement-scale etchings** — Extended #176 etching system with settlement: prefix. New stamps: settlement:pop:5/10/25/50/100 (tickPopulation crosses each threshold), settlement:raid:survived (first time getRaidLossFraction blocked a sweep fully), settlement:repair:first (first time performRepair fired). CharacterView gains 🏘️ Settlement group with its own bucket in the altar UI.
- **#197 Trade routes — periodic resource → coin** — Marketplace now has a tradeRoute schema. Every 5 min: if wood ≥ 40, take 20 wood → +5 coin. Same for stone (20→5) and hide (5→3). New tickTradeRoutes runs after consumption tick. Multiple trades fire same cycle if thresholds met. Per-building cycleMs throttle via run.tradeRouteLastAt. UI helper getNextTradeAt for future "next trade in N min" indicators.
- **#196 TownView Storage section** — New 📦 Storage section in TownView between Production and Arcane. Filters buildings with storageCaps field. Existing Silo + Cairn + new Granary/Cistern/Lumber Stack now group together cleanly instead of being scattered across Production / Comfort / Other.

- **#195 Dev panel + docs catchup for #182-#194 economy arc** — Six new town-economy dev helpers in src/systems/dev.js: devStockResources (fills inventory to comfortable buffers), devForceStarvation (stamps food shortage at 6min for death-cascade testing), devEndStarvation (clears all shortages + restocks food), devDestroyRandomBuilding (raid simulator for testing repair flow), devClearDestroyed, devClearAssignments. New "Town economy" section in DevPanel Arcane tab with all six buttons + live counter row (destroyed / locks / shortages). Plus a consolidated "Town / Economy" section in docs/systems.md covering state shape, tick order, population growth math, production schema, hybrid staffing, consumption + starvation math, raid sweep math, repair flow, and TownView UI breakdown — single canonical reference for the entire #182-#194 arc.

- **#194 Building repair + net delta + storage caps (3-in-1)** — Three landed together. **(1) Building repair**: raid destruction now stamps `run.destroyedBuildings = { [id]: { destroyedAt } }`. New `canRepair`/`performRepair` in systems/building.js (50% original cost, rounded up to min 1). New `REPAIR_BUILDING` action + `actions.repairBuilding(id)`. TownView shows a 🔥 Damaged section with per-building Repair button + halved cost chips. **(2) Net delta indicator**: new `getNetProductionRates(state)` in town.js combines recipe outputs (+), recipe inputs (-), passiveProduce (+), and per-villager consumption (-). New 📊 Net row on TownView header lists per-resource flow with green for positive, red for negative. **(3) Storage cap buildings**: Granary (+50 food / +20 bread / +15 bird_meat / +10 eggs cap), Cistern (+30 muddy / +20 boiled / +10 stagnant water cap, requires Well), Lumber Stack (+50 wood cap, Era 1 — requires Hut only).

- **#193 Starvation teeth — unmet consumption hurts** — The consumption tick now tracks sustained shortage per resource (run.shortageMs + shortageLastLossAt). Escalating penalties: 0-60s grace, 60-180s sanity bleed -0.5/min per missing resource, 180-300s villagers leave (1 per 60s per shortage), 300s+ villagers die (1 per 60s per shortage). Shortage clears the moment the resource is met that tick and posts a "🌾 The X shortage is over." milestone. New `getShortageStatus(state)` helper for the UI. New starvation banner on TownView header escalates color: yellow (warn) → orange (sanity) → red (leaves) → dark-red (deaths), with the worst-affected tier displayed and per-resource shortage durations.

- **#192 Settlement consumption — population drains stockpile** — Settlement now burns food/water/wood passively. New `tickConsumption(state, now)` in town.js + `getConsumptionRates(state)` helper. Rates per villager per minute: food 0.3, water 0.3 (drains highest tier first: boiled → muddy → stagnant), wood 0.2. Fractional accumulator (consumptionAccum) carries sub-cycle drain between TICKs. Wired into TICK_LOOP after the production tick (so a worker can produce-and-be-consumed in the same tick without double-counting). 1-minute catchup cap. **Math tuning**: with 5 villagers, settlement consumes 1.5 food + 1.5 water + 1.0 wood per minute. 1 Sawmill worker produces 2 wood/min (net +1.0 wood). 2 Sawmill workers (4 wood/min) net +3.0 wood — clear surplus. Bakery + Garden + Water Hole have to outpace consumption for food/water. New consumption ticker on TownView header card: `🔥 Consumption (5 villagers): -1.50 food/min · -1.50 water/min · -1.00 wood/min` so the player sees the burn rate live.

- **#191 Brutal raid sweep — Watchtower + army gated** — Raids now sweep ~90% of inventory by default. New `effects.raid.sweepFraction: 0.9` schema. Sweep iterates run.inventory and skips protected keys (weapons, tools, consumables, arcane items — the player can still fight). Defense math redesigned in defense.js: `getRaidLossFraction(state, baseFrac)` — pre-Watchtower defensive buildings shave 4% per defense point off the base, then Watchtower applies a critical 0.35× multiplier, then army strength (weapons in inventory + Watchtower-staffed villagers × 3) reduces that multiplier by 4% per point with a 0.15× floor. New `getArmyStrength(state)` + `getRaidProtectedKeys(state)` exports. Without Watchtower: lose 90%. Walls only: 78%. Watchtower alone (no army): 31%. Watchtower + Walls + 10 weapons: ~16%. Watchtower + Walls + 20 weapons + 3 garrison: ~5%. Watchtower gained `staffSlots: 3` so guards are assignable. All 5 raid events updated with sweepFraction values (Bandit 0.6, Carrion 0.5, Cultists-refuse 0.7, Demon 0.95, Plague-trade 0.3). Log line: "🔥 Raid swept 16% of the stockpile — 12 wood, 8 stone, 4 fragments, 3 food."

- **#190 Settlement raids — building-targeting threats** — Five new raid events that finally make Watchtower defense matter. New `effects.raid: { stealResource: { id, amount }, damageBuilding: { count }, killVillagers: N }` schema in systems/events.js applyEffects. All three quantities scale inversely by `getDefense(state)`: each point of defense reduces effective intensity by ~7%, capped at 70% reduction. Building damage picks random non-shelter buildings and removes them from run.built (shelter excluded so housing cap never cascades). The events: Bandit Raid (Era 2, food + 1 villager), Carrion Crew (Era 2, coin + 1 building), Cultists Come Calling (Era 3, choice: refuse → 2 villagers + 1 building, OR appease → -10 fragments), Demon Incursion (Era 3, 3 villagers + 2 buildings + 8 fragments + alignment evil), Plague Caravan (Era 3, choice: turn back → small sanity hit, OR trade → +10 coin but kills 2). Each raid uses milestone log lines like "🛡️ Raid stole 15 food (defense 5 softened it)" and "🔥 Raid destroyed: Sawmill, Bakery."

- **#189 Civic buildings — Era 2/3 non-production layer** — Five new buildings filling the support/civic gap between raw production and arcane studies. Era 2: Watchtower (+5 defense, gated on walls), Moot Hall (+50% population growth, multiplicative), Marketplace (1 villager: 5 food → 1 tarnished_coin/min). Era 3: University (+25% study speed, gated on scriptorium), Mint (+0.5 tarnished_coin/min passive). Two new effect schema fields wired in: `effect.populationGrowthMult` (read by new `getPopulationGrowthMult` in town.js, multiplied into `tickPopulation` integrator) and `effect.studySpeedMult` (read by new `getStudySpeedMult` in studies.js, multiplied into `tickStudies` elapsed window). Both are multiplicative across owned buildings, default 1.0. Marketplace + Mint give the player a coin economy without grinding mug runs. University meaningfully accelerates the arcane research grind.

- **#188 Population events — wanderers + villager loss** — Six new events.js entries that pump gainPopulation/losePopulation through a new `effects.population: ±N` field in systems/events.js applyEffects. Set: wandererArrival (+1, Era 1+), refugeeFamily (+3 or turn-away choice, Era 2+), villagerLost (-1, Era 1+), plagueOutbreak (-1 quarantine vs -3 let-run choice, Era 2+), villagerSeeksFortune (-1 with blessing vs force-stay alignment choice, Era 2+), travelingCaravan (+2 with food + tarnished_coin bonus, Era 2+). Each event includes appropriate sanity/happiness/alignment effects + flavor log. The `effects.populationReason` field threads a reason string into the gain/lose log lines.

- **#187 Economy chunk 5 — manual staffing override** — Hybrid assignment model. New `run.assignments = { [buildingId]: { locked: N } }` state. `getAutoAssignments` renamed to `getAssignments(state)` (back-compat alias kept) — applies locked counts first (clamped to staffSlots, capped by total pop), then round-robin auto-fills the remainder into unlocked buildings only. New `setBuildingAssignment(run, buildingId, count)` system fn + `SET_BUILDING_ASSIGNMENT` reducer case + `actions.assignBuilding(id, count|null)` dispatcher. TownView BuildingCard now shows a staffing control strip on built production buildings: −/+ buttons clamp [0..staffSlots] and stamp a lock, "locked N/M" vs "auto N/M" label, and an "Auto" reset button when a lock is set. Subheader chip shows 🔒 when locked. count==null clears the lock.

- **#186 Economy chunk 4 — Town view** — New TownView center-view component consolidates the population + housing + production systems. Shell.jsx now routes view==="world" to TownView when computeEra(state) >= 2, falling back to the legacy ActionPanel (Wasteland) for Era 0-1. Layout: Population header card showing N/M villagers + growth status (🌱 Growing / ⚠️ Stalled with specific blockers like "need food (3/5), sanity (24/30)" / At capacity), then building sections grouped by category (Housing / Production / Arcane / Comfort / Other). Each BuildingCard shows built vs unbuilt state, live staffing (👥 N/M), live output rate per minute (recipe or passiveProduce or effect.spiritPerMinute/sanityPerMinute), or a "No villagers — staffing stalled" hint. Unbuilt cards show cost chips with have/need color coding + a Build button gated by canBuild reasons. Auto-refreshes every 1s so the output rates feel alive without spamming TICK_LOOP.

- **#184 Economy chunk 3 — Era 3 magical production** — Five new arcane buildings gated on Stone Altar + altarWork research. Three productionRecipe-driven: Apothecary (2 frag + 3 food + 2 muddy water → 1 Potion of Mending / 5min), Scriptorium (2 torn_page + 1 ink → 1 scroll / 4min), Rune Forge (4 frag + 3 stone → 1 Splinter Rune / 10min). Two passive-trickle: Spirit Censer Workshop (+0.5 spirit/min) and Temple (+0.8 sanity/min) using the same `effect.spiritPerMinute` / `sanityPerMinute` hook the Stone Altar already used. All five reuse the #183 productionRecipe + staffSlots schema with no system changes — the same `tickRecipeProduction` handles arcane and mundane production identically.

- **#183 Economy chunk 2 — production buildings + auto-staffing** — Five new Era 2 production buildings: Sawmill (2 staff, +2 wood/min/villager, no input), Quarry (2 staff, +2 stone/min/villager), Bakery (1 staff, 2 food → 1 bread/min), Tannery (1 staff, 1 hide → 1 leather/min), Brewery (1 staff, 1 boiled water + 1 food → 1 ale per 2 min). Three new resources: bread (food, nutrition 30, slow spoilage), leather (material), ale (drink + sanity buff). New `productionRecipe: { input, output, perVillagerPerMinute }` + `staffSlots` schema fields. New `getAutoAssignments(state)` in systems/town.js distributes idle population round-robin across production buildings, capped by staffSlots. New `tickRecipeProduction(state, now)` consumes inputs + produces outputs scaled by assignment, with 1-minute catchup cap. Wired into TICK_LOOP after tickPopulation. BuildingsTreeModal now shows a "👥 Staffed: N/M" block on production buildings with live input/output rate per minute (or a "No villagers — production stalled" hint when unstaffed).

- **#182 Economy chunk 1 — population + housing** — Foundation for the town/economy arc. New `run.population` + `populationGrowAccum` + `lastPopulationTickAt` fields. New `systems/town.js` with `tickPopulation` (called from TICK_LOOP after `tickWorkers`): integrates +1 villager every 5 min when food ≥ 5 + water > 0 + sanity ≥ 30, capped by housing. New `housing` field on buildings: Hut=1, Home=2, Lean-to=1, Cottage=3. Two new shelter buildings (Lean-to gated on Hut, Cottage gated on Home + home research). Population row added to SurvivalBars (under Defense) showing `current / cap` with at-cap tooltip. Helpers in town.js: `getHousingCap`, `populationGrowthEnabled`, `gainPopulation`, `losePopulation` for future event hooks. Dev panel: 🏠 Set Population 10/0, 🛖 Build all Era 1-2 shelter. No production buildings yet — that's chunk 2.

- **#181 Mug targets per-era expansion** — Each of the 8 role types now has 3 era tiers (24 total mug targets). Loot tables scale: Era 1 = food/wood/water_stagnant, Era 2 = bronze/iron/muddy water/tarnished_coin, Era 3 = fragments/coins/scrolls/runes. Difficulty + XP + alignment evil + failure penalties also escalate. Each tier gets a flavor-name (e.g. Peasant → Settler → Townsfolk; Smith → Knapper / Smith / Runesmith; Merchant → Drifter / Caravaner / Guild Merchant). UI auto-wired — PatrolView reads getMugTargetsForEra(era) so each era shows all unlocked tiers as cards.

- **#180 Thievery skill + mug targets** — New gameplay system. Thievery skill added to content/skills.js (active, combat category, max lv 20, bonuses: +1% mug success per level cap 25%, +1% loot chance per level cap 20%). 8 mug targets across the 3 eras: Peasant / Hunter / Woodcutter / Farmer (Era 1), Fisherman / Miner / Smith (Era 2), Merchant (Era 3). Each target has a discipline-aligned loot table (peasants give food/wood/water, smiths give iron/stone, merchants give coins/fragments), per-target difficulty + alignment evil gain + failure penalty (HP / sanity hits scaling to target). systems/thievery.js owns canMug + performMug; reads thievery level + DEX into a success roll, alignment.evil += target.alignmentEvil on success, applyEffect(hp/sanity) on failure. New THIEVERY_MUG action + reducer case + actions.mug(targetId). UI: new "Mug" tab on PatrolView next to "Foes" (uses the #173 tab fade), MugCard mirrors the patrol-card layout with loot preview + fail% chip. Etching stamp on first mug per target (mug:<id>:first → Combat group via the existing #176 prefix mapping). Dev panel: 🗡️ Set Thievery Lv 10 / Lv 20 helpers. New run state: run.mugsLanded = { [targetId]: count }.

- **#179 Weapon effective-stats tooltip** — Filled weapon slots on the Character page now show a multi-line breakdown in the hover title. Lines: base (rolled dmg range, acc, crit) → +skill (combat-skill bonuses by level) → +stats (STR/DEX/MAG damageBonus/accBonus/damageMult) → +runes (count + each rune's effect label) → +blessings (active count) → +enchants (count + each enchant's effect label) → "= Effective" totals (rolled-up dmg range × damageMult, summed acc + crit). Implemented as `buildWeaponBreakdown(def, state)` in CharacterView.jsx, reusing the canonical combat helpers (`getCombatSkillForWeapon` + `getCombatSkillBonuses` + `getStatCombatBonuses` + `getEffectiveImbueEffects`) so the tooltip always matches what `resolveFight` will roll. Only renders for slots where `def.weaponStats` exists (handLeft / handRight / ranged). State now passed through Slot prop drilling.
- **#178 Docs + dev panel catchup** — This entry + systems.md "Stone Altar etchings" section refresh with new id-prefix table + ascension+craft+hunts+combat groups. Dev panel gets a sampler that stamps one etching per group (so the new Character → Altar UI can be smoke-tested instantly) and prefix-clear buttons for each group. Voidmark testing helper (one-click Evil 5).
- **#177 Voidmark + enchant tooltip polish + save migration check** — `canEnchant` reason strings now name the specific gate: study + path ("Requires completing the \"Greater Mending\" study (The Light Path).") and alignment threshold + current value ("Requires alignment: 5 evil (have 2)."). Old saves load fine — verified via `scripts/smoke_migrate.test.mjs` which confirms `RUN_DEFAULTS` spread populates the new `enchantments`/`weaponImbues`/`blessings` fields as empty objects.
- **#176 Altar etching content expansion** — Six new stamp hooks via shared `systems/etchings.js` (`stampEtchingOnce` + `isFirstStamp` helpers). Triggers: first prey species hunted (`prey:<id>:first` from hunting.js), first mob species slain (`mob:<id>:first` from patrol.js), first weapon-tier craft (`craft:weapon:<category>:first` from crafting.js), first rune inscribed (`craft:rune:first`), first enchant etched (`craft:enchant:first` from enchantments.js), each ascension (`ascension:N` from PRESTIGE reducer case). CharacterView etchingMeta() extended with new id prefixes + four new group buckets (Ascensions / Combat / Hunts / Crafts). Each stamp also pushes a "🕯️ An etching appears on the Altar" milestone event to the log on its first occurrence. All hooks are idempotent — already-stamped ids no-op.
- **#175 Era 1 bird tiering + grub-bird pest interaction** — Three new Era 1 birds: Dust Wren (easy/common, grub-eater), Scrub Finch (uncommon, grub-eater, drops bird_eggs), Gristle Vulture (rare/apex, drops sinew + 10% fragment chance). New `bird_eggs` resource (food, nutrition 8, baseCap 8, spoils fast). New `feedsOnGrubs: true` field on prey passes through `rollPreyDrops` in `systems/hunting.js`: when `activePests.birdFlock.until` is in the future, those birds get +15% drop chance and a 50% chance to drop 1-2 bonus grubs ("food bonus from the flock"). Era 1 prey roster grew from 4 → 7 (4 mammals, 3 birds + Wind Sparrow = 4 birds total). UI auto-wired — HuntingView reads getAllPrey() and renders the new cards without code changes.
- **#174 Stone Altar etchings UI** — Flipped the 🟡 "foundation laid" entry to 🟢. New "Altar" section on the Character page renders `persistent.altarEtchings` grouped by source (Foundation / Studies / Bosses / Other), each etching showing icon + label + relative stamp time. Jump-nav entry added. Empty / no-altar placeholders. Etchings persist across prestige (trophy-wall arc).
- **#173 #49 polish** — Tab-panel fade-in (220ms keyframe, key={activeTab} remount on Magic + Crafting), patrol-card + btn-primary hover lift with layered shadows, equip-flash on char-slot-value (700ms amber pulse keyed by item id), pulsing red overlay on .sb-bar--danger .sb-bar-fill (HP/Sanity/Spirit). All wrapped in prefers-reduced-motion. ~+1.6 kB CSS.
- **#170 (#37) Weapon enchants tied to Arcane Studies** — Permanent, study-gated mark system. Distinct from runes: separate slot budget (primitive=1, bronze=2, iron=2, arcane=3), cannot be removed. Each Arcane Studies path unlocks one enchant (Mending Aura / Blessing Mark / Drain Sigil / Dominance Ward / Greenward / Truestrike Sigil / Echo Weave / Wardward / Voidmark). Cost: 8 fragments + 30 Spirit (Voidmark scales to 20+60). Combat math: `getEffectiveImbueEffects` in `systems/combat.js` now folds enchantment effects in alongside runes + blessings, so a fully kitted weapon stacks all three. Reducer case `ENCHANT_WEAPON` + `actions.enchantWeapon(weaponId, enchantId)`. UI: new Enchantments section on each WeaponImbueCard in Magic → Runesmithing tab, with a "Permanent" confirm dialog before etching. State shape: `run.enchantments = { [weaponId]: { [enchantId]: { appliedAt } } }`. New files: `content/enchantments.js`, `systems/enchantments.js`.
- **#171 Dev panel helpers for enchantments** — ArcaneTab section under Runesmithing helpers: 🪬 Etch all unlocked enchants on every weapon, 🪬 Clear all enchantments, 🕯️ Build Stone Altar (enchant gate), 📚 Complete all studies, ✨ +50 Arcane Shards. Counter row shows current enchanted-weapon count.
- **#172 Docs catchup for #170/#171** — This entry + new "Weapon enchantments (#37 / #170)" section in systems.md + BUGS.md entry for the OneDrive truncation that hit five files mid-session.
- **#138 Enchant slots + boss rune drops** — Per-weapon-category enchant cap (primitive=1, bronze=2, iron=3, arcane=4) gates `canImbueWeapon`. Each of the 6 bosses got a `runeDrops: [{resource, chance, qty}]` field rolled on victory in `performBossFightEnd`. Era 1 bosses drop Common, Era 2 → Rare, Era 3 mini → Epic, Era 3 main → Legendary + 6% Mythic. UI shows `🪬 N/M ●●○○` slot pips on Magic-tab Runesmithing cards.
- **#139 Boss Challenges modal restyle** — `BossPicker` rebuilt as patrol-card grid: icon + name + era/tier chip + stats section (HP/acc/dmg) + possible-rune-drops section with rarity-colored chips and %. `🥇 beaten` chip + 0.72 opacity for already-defeated bosses.
- **#140 Boss-fight layout** — Two-column arena. Left = player portrait/bars/wielding/quick-consumables/action stack. Right = foe portrait/bars/stats. Combat log spans full width below. Each side `min-height: 380px` so outros don't shift columns.
- **#141 SpellPicker/ItemPicker cleanup** — Bulleted lists retired; each row is a single full-width clickable button. Effect chips (`+20 HP`), cost chips (`✨1 🌀15`), multiline tooltip with description + cost + cooldown.
- **#142–#143 Boss modal re-engage + crafting continuous loop** — Boss modal `Return` now fully closes (cleanest lifecycle). Crafting loop runs indefinitely (`queuedQty: 0` sentinel) until materials exhaust or Stop pressed; "swap" cancels current and starts new.
- **#144–#145 Crafting card polish** — Loopbar lives at the bottom of the card matching the gather pattern. Button is uniform `.patrol-card-cta--action` size (same as gather CTA). Dropped the `×N` owned suffix.
- **#146–#149 CSS truncation hardening** — OneDrive sync was clobbering brace closures during rapid Edit cycles. Multiple recoveries from HEAD plus an explicit BUGS.md #019 entry. Build verified after each round.
- **#150 Per-era body class** — `document.body[data-era="0|1|2|3"]` set from Shell on era change. Each era gets a subtle radial-gradient tint: dust → warm hearth glow → settled gray → arcane violet.
- **#151 Bless action** — Phase 2 of #115. Burn 1 rune + 10 Spirit → 5-min temporary buff. New `run.blessings: {[runeId]: {expiresAt}}`. `getEffectiveImbueEffects` aggregates BOTH weapon imbues AND active blessings, so combat math is unified. `tickBlessings` clears expired ones on TICK. UI: `🕯️ Bless` button next to Bind on every rune; "Active Blessings" card at top of Runesmithing tab with mm:ss countdown.
- **#152 Equipment tooltip-compare (#46)** — Hovering an EquipmentInventoryGrid card shows a multi-line `title=` tooltip with stat deltas vs whatever's in the matching slot. Weapons compare damage avg / acc / crit. Armor compares armor stat. Rings get a "10 slots available" hint.
- **#153 No-shelter rest penalty** — Resting with no hut/home halves energy gain and costs 2 Sanity. Flavor message swaps to *"You curl up against the dust. Cold finds you in your sleep."*
- **#154 Dev panel: rune/bless/imbue helpers** — New Arcane-tab section with grant-by-rarity (Common/Uncommon/Rare/Epic/Legendary/Mythic = 5 each; God = 1 each), force-bless lightRune/voidRune buttons, clear-all-imbues + clear-blessings dangers. `devGiveRunesByRarity`, `devForceBless`, `devClearBlessings`, `devClearImbues` in systems/dev.js.
- **#155–#158 Boss modal lifecycle fixes** — `initialBossId` prop is guard-railed against re-syncing if the player picked a different boss after Return. Shell's patrol-encounter `devPatch` uses a `stateRef` to avoid clobbering live state. Boss patrol-card clicks bypass the auto-loop entirely (stamp `patrolBossEncounter` immediately so the modal opens without the 12s wait). Final fix: Return fully closes the modal so the next engagement starts from a fresh mount.
- **#157 Boss-fight auto-attack + countdown** — Clicking Attack toggles auto-attack mode; swings cycle every 6s until win/defeat/flee/Stop. Countdown number overlays the player portrait; scales 28px → 52px, turns orange, pulses 1s in the final 5s via `bossCountdownPulse` keyframe. Picking Spell/Item/Defend/Flee turns auto-attack off.
- **#156 Fair loop timer** — `setActiveLoop` no longer biases `startedAt` backward by one cycle. Click → full cycleMs must elapse before the first cycle fires → cancellation before completion grants no kill/drops. Applies to all auto-loops (patrol, hunt, gather).
- **#159 Quick eat + spell category tabs** — In-fight QuickConsumables panel for one-click food/item use without opening the picker. SpellPicker bucketed into Heal/Buff/Attack tabs (inferred from effect shape — positive hp/sanity/spirit = heal, targetsFoe/damage = attack, otherwise buff).
- **#160 Bars + chip-list quick-consumables** — `.boss-bar` got proper styling: 60px label / flex track / 60px num grid, color-graded gradient fills per accent (HP red→orange, Sanity blue, Spirit purple, Foe dark red) with `transition: width 0.35s ease` so stats slide smoothly when they change.
- **#161 Split-button Eat/Item** — Two pill-shaped split buttons matching the user's mock. Main button uses the best food/item (highest nutrition / hp restore). Right side is a `▾` arrow opening a popover listing every owned consumable. Keyboard shortcuts `E` (eat best) and `I` (use best item).
- **#162 Action stack cleanup** — Item button dropped from the action row (Attack / Spell / Defend / Flee) since the split-button above covers it with a shortcut.
- **#163 Full boss stats mid-fight** — Foe column now shows the same Stats + Possible Rune Drops panels that appear on the boss-picker card. HP updates live as the player damages the boss.
- **#164 Spell tab CSS restoration** — `.boss-subpicker-tab` rules got lost in a truncation cycle. Re-added with the dark-pill / accent-border styling that matches the path-tree tabs elsewhere.
- **#165 Hide unavailable styles + draggable boss modal** — `CombatStylePicker` filters out styles you can't currently use (only the currently-selected one stays visible even if locked). Boss modal is draggable from the title bar via the new `useDraggableModal` hook + resizable via native `resize: both` with a visible corner grip.
- **#166–#167 Tree modals: drag + resize + click-safe** — `StudyTreeModal`, `BuildingsTreeModal`, `TeachingsTreeModal` all use the shared `useDraggableModal` hook. Overlay close switched from `onClick` to `onPointerDown` so resize-release outside the modal doesn't trigger close.
- **#168 Tree SVG fills resized modal** — Chained sizing rules scoped to `.modal--tree.modal--draggable` make the body flex-grow, the canvas fill, and the SVG drop its `max-height: 520px` cap so the tree expands into the new space when the user pulls the corner.

### Recent progress (#109–#137)

- **#109 Crafting page rebuild** — sub-tabbed CraftingView with 8 disciplines (Survival / Blacksmithing / Alchemy / Fletching / Woodworking / Tailoring / Farming / Runesmithing). Patrol-card visual language. Per-card cost row + Craft button always visible (no "Effects active" hide).
- **#110–#111 Durability rebalance** — Era 0/1 net 12→6, snare 20→10, etc. Cards show `cur/max hunts` cleanly.
- **#112–#113 Per-discipline skills + skill-based success** — blacksmithing/alchemy/fletching/woodworking/tailoring/survivalcraft/runesmithing/smithing. Each level adds 4% success above the tier base (T1 25% / T2 20% / T3 12% / T4 8%); ceiling per tier (95/90/80/70).
- **#114–#117 Era 2 + Era 3 weapon rosters** — 14 Era 2/iron-tier natural weapons + 24 Era 3 arcane weapons (Fragment Blade, Sigil Staff, Echo Bow, Voidcaller Wand, Stonespeak Hammer, Censer Dagger, …).
- **#119–#120 Mythical mob roster + drops → recipes** — replaced 5 mundane mobs with mythical (shardChimera, whisperingCairn, thirstWraith, ironwombBrood, mirrorGhast) plus 3 new Era 3 mobs (harvestingAngel, echoFiend, duneLeviathan). Era 3 weapon recipes now require mythical mob drops (shattered_glyph, spirit_essence, void_bone, cherub_feather, etc.). Hunt-craft loop closed.
- **#122–#127 Crafting QoL** — skill-based cost discount (1%/lvl, cap 30%), allow crafting non-stackables repeatedly, fletching arrow recipes (wood/iron/shard tiers), Survival tab + reclassification, ×1/×5/×10/Max quantity selector, lower 25% base success, `+` button to auto-craft missing materials.
- **#130 Timed crafting** — every craft is now a progress-bar with per-tier base durations (T1 5s / T2 15s / T3 60s / T4 180s) modulated by discipline-skill level. `activeCraft` lives on `run`; `tickActiveCraft` resolves on TICK_LOOP. Multi-craft queue auto-restarts the next one when materials remain. The idle-RPG loop the user asked for: queue a Voidstaff, walk away, come back to find it done.
- **#36 / #131 Iron tier + Smithing skill** — `iron` resource (hidden until smithing research), `smeltIron` recipe (`bog_iron×2 + wood×1 → iron×1`), iron-tier crafts grant Smithing XP. Existing iron-tier weapons (#117) now have a real resource chain.
- **#115 / #132 Runesmithing + imbue actions** — `weaponImbues: { [weaponId]: { [runeId]: { appliedAt } } }` field on run. Per-weapon-type imbues. `IMBUE_WEAPON` / `REMOVE_IMBUE` reducer actions. `canImbueWeapon` / `getWeaponImbues` / `performImbueWeapon` / `performRemoveImbue` in `systems/runesmithing.js`. Imbuing grants 8 Runesmithing XP.
- **#133 Combat math wired** — `getEffectiveImbueEffects(state)` aggregates `damageBonus / hpReturnOnHit / spiritReturnOnHit / echoChance / sanityCostOnHit / durabilitySaveChance / hpRegenPerMinute` plus #136's new `accBonus / critChanceBonus / spiritRegenPerMinute / damageReduction / evasionBonus`. Applied in both `resolveFight` (auto-patrol) and `rollPlayerAttack` (boss modal). BossFightModal applies on-hit returns + echo strike to the damage tracker live.
- **#134 Elemental rune passive regen** — `applyImbuePassives(run, tickSeconds)` honest fractional accumulator on `imbueRegenAccum` / `imbueSpiritAccum`. Hooked into `ACTIONS.TICK` (15s). Caps stats at 100 and zeros the accumulator at cap.
- **#135 Runesmithing UI on Magic page** — new 🪬 Runesmithing tab in MagicView (visible when player has runesmithing skill level OR owns any rune). One patrol-style card per owned weapon with weaponStats. Shows currently-bound imbues with × remove buttons + an Apply-rune list of every rune type with Bind buttons and disabled-with-reason tooltips.
- **#136 50 new runes across 7 rarity tiers** — `rarity: common | uncommon | rare | epic | legendary | mythic | god`. Common → small bonuses (e.g. Mending Chip +1 HP/hit). God → world-bending (Godrune of Oblivion: +50 dmg, −10 Sanity/hit, 60% echo, +20% crit, +20% acc). All sort by rarity in the UI; rarity-colored chips on each row (gray → green → blue → purple → gold → orange → rainbow GOD). Combined imbue caps: `damageReduction ≤ 0.90`, `echoChance ≤ 1.0`, `durabilitySaveChance ≤ 0.95`.
- **#137 50 rune craft recipes** — full coverage. Cost ladder scales by rarity: Common (1f/1ink) → Uncommon (2f/1ink + 1 themed) → Rare (3f/2ink/1obol + 1 themed) → Epic (5f/3ink/2obol + mythical) → Legendary (8f/5ink/3obol + 2 mythical + sometimes 1 starlit) → Mythic (15f/8ink/5obol + 3 mythical + starlit) → God (30f/15ink/10obol + multiple mythical + 2–5 starlit). God-tier Void runes additionally gate on `voidcall` research + `alignment.evil ≥ 6–12`.

### Playable end-to-end
- **Era 0** (once per save; skipped after first ascension) — gather → find rock → 10 fragments → awakening
- **Era 1** — hut → research tree (16+ nodes) → Fire Pit · Water Hole · Garden · Cairn → primitive tools + pure weapons (Wooden Club / Stone Spear / Stone Mace) → water tier system (Stagnant 🩸 / Muddy 💧 / Boiled 🫖 + dysentery + Drink dropdown + Boil action) → multi-round combat (Wild Dog) → hunting → six survival stats (HP/hunger/thirst/energy/Resolve/Sanity/Spirit)
- **Era 2** — Smithing + Forge + Era-2 dual-use tools (Stone Axe / Pickaxe / Bone Knife / Bow w/ `weaponStats`) → Fletching → Home + Stone Walls + Silo + Farmhouse settlement chain → Raider combat-class threat → Altar Work → Stone Altar → Scroll 📜 / Ink 🖋️ via `producesResource` craft pattern → prestige UI reveals
- **Era 3** — Arcane Awakening reveals fragments as Arcane Shards → 7 magic paths × 21 study nodes (timed, pause-on-action 5s idle, lossless switching, cross-path Wardweave/Ghostcall/Truesight, apex-gated Voidcall on `alignment.evil ≥ 5`) → 10 study-gated spells via `requires.studied` → Whisperer / Hollow Hound / Iconoclast / Corrupted Walker / Soulless Stalker (sanity-damage, ignores armor) → Fragment Knife / Spirit Censer / Warding Talisman arcane tier → Ritual action (fragments → Spirit) → altar etchings persist via `persistent.altarEtchings`

### Cross-cutting systems (shipped)
Per-system status in `docs/systems.md`. Grouped highlights:

- **Foundation** — persistent/run split · thin reducer + pure systems · content-as-data · scene composition · versioned save migration · settings/audio/keyboard shortcuts (G/R/E/D/H, customizable) · splash screen · save export/import
- **Combat** — equipped slots (8 main + 13 accessory) · weapons (`weaponStats`) · multi-round fight loop · armor/defense split · `damageType` routing · death-debuff cascade · per-fight `applyToolWear` · combat skills (swordplay/archery/magicCombat) · boss-fight turn-based modal (#40) · Patrol loop (#66) with 44 mobs in `content/mobs.js`, idle auto-loop runner (#68), mob-card view with reveal milestones (#70), per-target Pile-of-Goods with hover tooltips (#69), butchering skill (#70) scales drop chance + qty, knowledge-gated boss spawns in patrol pool · **Unarmored penalty (#72): `getArmoredCount(run)` reads 5 main armor slots (head/chest/leggings/boots/gloves); `getUnarmoredPenalty(state)` returns `{accPenalty, dmgMult}` applied in `rollPlayerAttack` / `rollFoeAttack` / `resolveFight`. Curve: 0 pieces = −25% acc, +50% dmg taken / 1–2 = −10/+25 / 3–4 = −5/+10 / 5 = none. PatrolView shows a red warning banner "🩸 Underdressed: −X% accuracy, +Y% damage taken" when active.** · Town workers (#71): `townWorkers` echo upgrade (5 lvls, 8 echoes base, 1.5× scaling); each level hires a townsperson, 60s cycle, 75% win rate, drops at 0.5× qty, Era 1 mobs only. `🛠 × N` badge in PatrolView pile header.
- **Meta** — Echo Shop (15 upgrades, 6 categories: Cache/Body/Mind/Skills/Arcane/**Town**) · ascension QoL · 50 random events across Era 1/2/3 · World Score hidden meter · dysentery + disease module · prestige system with reward breakdown · **Town workers (#71)** — `townWorkers` echo upgrade (5 levels, baseCost 8 echoes, 1.5× scaling). Each level hires one townsperson who passively patrols Era 1 mobs every 60s (per worker) at 75% win rate. Drops at 0.5× quantity flow into inventory. Fires from TICK_LOOP via `systems/workers.js`; cheap when count=0; catches up to 30 min offline. Worker badge (`🛠 × N`) renders in PatrolView pile header.
- **UI Layout** — left rail (icons only, no lc-content panel) · center column (view-routed) · right column (off-canvas) · footer ActionStrip · Stone strip · dev panel (6 tabs)
- **Left rail (rail-as-nav)** — top group: 3 view-switcher icons (🌍 World / 👤 Character / 🛠️ Crafting) above a divider. Bottom group: content tabs (✨ Arcane / 🏛️ Buildings / 🕯️ Studies). Skills + Inventory retired (#45 + #54) → Character page. Body & Mind + 🔨 Tools retired earlier. **Challenges retired (#66)** — bosses now spawn from Patrol when knowledge-gated. Each icon `title` carries the lead-text blurb as a tooltip.
- **Center routing** — `view` state in Shell drives `ActionPanel` / `CharacterView` / `CraftingView` / `ArcaneView` / `StudiesView`. SkillsView + InventoryView routes removed (their components now orphan files). Buildings + Challenges rail icons still pop their existing modals (inlining deferred).
- **Right column (Recent / Unlocks / Stats)** — 3-mode off-canvas (`grid | closed | overlay`). ‹ close inside; ‹ edge tab when closed. Persists via `lithos.rightPanelMode`. Overlay degrades to closed on reload.
- **Character page hub (#44 → #45 → #54)** — single-page hub with 4 anchored sections + sticky jump-nav (📊 Stats / 🎯 Skills / 🛡️ Equipment / 🎒 Items). Sticky strip sits left-edge on wide viewports (≥1101px); flips to a horizontal sticky bar with `backdrop-filter: blur(4px)` below 1100px.
  - **Stats** — two columns (Survival incl. Bridge STR via in-column divider | Combat). STR proxied by death-debuff inverse `10 − floor(mag×10)` until #47. DEX/SPD/MAG placeholder dashes until #47. Armor reads `getPersonalArmor(state)`.
  - **Skills** — Survival-skills + Combat-skills columns mirror the stat grid above; Craft/Arcane/Industry fall into a full-width "Other" row when active. Reuses `.skill-row` CSS.
  - **Equipment** — 8 main slots + collapsible accessory tray (back/overArmor/talisman/10 rings). Filled slots are clickable buttons (`actions.unequip(slot)` / `actions.unequipRing(i)`); two-handed off-hand pointer unequips both hands.
  - **Items** — `EquipmentInventoryGrid` (#45): top tabs All/Weapons/Defense/Magic/Tools/Crafting/Herbs/Other; cards show icon/qty/cap/weapon stats inline/description/durability bar. EquipButtons routes by `weaponStats.type`: melee → L/R, ranged → Ranged, two-handed → Both hands. Consumables get Use; hidden resources render as "???" with no actions. Spare-aware (own 2 → equip one to one hand, the spare to the other).
- **Modals** — Spells / Tools / BossFight / Echo Shop / Prestige / Reset / Settings / Event / BuildingsTree / StudyTree / TeachingsTree. SpellsModal exports `SpellsBody` for inline reuse (ArcaneView).
- **Tooltips** — native `title` attribute on stat rows, study rows, and all rail icons.
- **Active-study indicator** — 1s live-extrapolated progress bar in Stone strip; bar moves smoothly between 15s TICK commits.
- **Crafting view** — still a stub; #48 fills it with sub-tabbed disciplines.
- **Gather view (#97 → #104)** — unified center page replacing the old standalone HuntingView. Top tabs: Forage / Mining / Wood / Fishing / Farming / Husbandry / Hunting. Each tab is a `patrol-card` grid of `gatherNodes.js` entries (20 nodes across 6 disciplines, each w/ id/name/icon/discipline/skill/era/tier/drops/xp/cycleMs/flavor). Click a card → auto-loop runs `performGatherNode()` (or `performHunt()` for Hunting tab) every `cycleMs`. Drops accrue into a **shared Pile of Goods** (#69 pattern). Loop banner sits above the Pile (#104) — shows the node icon + proper display name + tier chip + progress bar + Stop button, instead of the raw camelCase id.
- **Magic view (#101 → #106)** — single tabbed page. Cast + Spellbook + Conversions merged. **Tab order mirrors the Path Trees modal exactly** (Foundation / ✨Light / 🌑Bend / 🌿Elemental / ✒️Sigilcraft / 🔔Memory / 👂Stoneword / ⚫Voidcall / 🪔Conversions). Bucket map (`SPELL_PATH` in `MagicView.jsx`) derived from `STUDIES[].effect.unlocksSpell`. Empty paths hide their tab. Spells render as **patrol-card tiles** for visual parity with Patrol / Hunting / Gather: icon, path-colored tier chip, description, cost chips, full-width Cast CTA. Ritual lives in Conversions as a synthetic spell.
- **Path Trees modal styling (#105)** — `.study-tab` pill buttons now match the project's dark theme (transparent + accent border on active) instead of browser-default white.
- **Era labels (#105)** — `eraLabel()` helper in PatrolView/GatherView renders "Era One / Two / Three" instead of "era 1" on all card sublines.
- **Building skill (#102)** — new `craft`-category skill in `content/skills.js`. Reduces survival cost of building actions (`buildEffortReduction` bonus, 0.03/lvl, cap 0.5). XP granted on `BUILD` actions. Some research nodes (fire, hiddenStores) now carry a `skillReq: { building: N }` precondition checked by `canListen()`.
- **Skills under Character (#107)** — Survival/Combat columns at sm+ breakpoint; Craft/Industry/Arcane each get their own column via `--cols` (no more "Other" pile). Mobile-first cascade. Rows get horizontal padding so bars/text don't kiss the column edge (#103 carry-over). Duplicate `.skill-row` CSS block (~100 lines) removed.
- **Breathing room pass (#103)** — `.action-panel`, `.patrol-card`, `.ei-card`, `.magic-row`, and skill rows all get explicit horizontal padding so text never touches container borders.

### Locked design decisions
- **Spirit = magic-energy stat** (was reserved as Mana; locked active in Era 3+)
- **STR = bridge stat** between survival and combat (lands at #34; death-debuff magnitude is the proxy until then)
- **Armor vs Defense** — armor reduces personal combat hp damage; defense is settlement-only (raids, food theft). Walls don't help when a dog jumps you in the wilderness.
- **Boss fights = turn-based modal** (shipped #40). Routine combat = passive log (shipped). Mid-fight spells/items dispatch real `CAST_SPELL`/`USE_TOOL` so spirit/fragment/cooldown bookkeeping stays single-source-of-truth; modal commits damage taken + outcome in one `BOSS_FIGHT_END` dispatch at fight end.
- **Voidcall apex-gated** by `alignment.evil ≥ 5`. Each Voidcall costs `worldScore −1`.
- **Ascension starts Era 1** (rock awakened + hut raised). RESET_RUN (death/give-up) still starts Era 0. The cosmic-horror opening hurts when you've earned it.
- **Resources stay known across runs.** Anything revealed before ascending stays revealed forever via `persistent.permanentlyKnown` (generic; works for any future hidden resource).
- **Skills are run-only.** Echo Shop's "Memory" upgrades seed start-of-run levels; XP never carries.
- **Seven magic paths, not three** (locked via AskUserQuestion). Cross-path nodes encourage build identity.
- **Combat-class vs one-shot threats coexist** — `threat.combat` field flips to fight-loop; threats without it stay narrative-rich one-shots. `routeThreat()` in `systems/threats.js` dispatches.
- **Arcane Studies layered on Stone's Teachings**, not replacing. Teachings = instant-listen for fundamentals. Altar = deep magic with timers.
- **Multiple in-progress studies allowed**, lossless pause-on-action, free switching. Materials are the cost; time is yours to hold.
- **Rail-as-nav** — left rail is icon-only. No lc-content panel. Each icon swaps the center view (preferred) or pops the corresponding modal (Buildings tree + Challenges boss, deferred inlining). Blurbs live as `title` tooltips. Header stays minimal: title + era + Echoes only.
- **Off-canvas right only** — right column can close/overlay; left rail is always visible. Choice persists via `lithos.rightPanelMode` (no Bootstrap dependency).

## Next moves (suggested order: #44 → #35 onward)

**Combat arc — open:**
- **#37** — weapon enchants tied to Arcane Studies. The Runesmithing/imbue system (#115/#132/#136/#138/#151) effectively delivered the enchant payload; what's still open is the **Altar-UI second pathway** — a dedicated screen at the Stone Altar where Light/Bend/Elemental study completions unlock additional enchant categories that don't slot into the rune system. Decide on slot/cost model before building.

**Character / Crafting polish:**
- **#49** polish — tab transitions, hover states, equip flourishes, damaged-stat red overlay on stat bars. *(#46, #48 shipped.)*

**Polish / paper-cuts:**
- Bird tiering / grub birds (Era 1 hunting depth) — content
- SFX expansion (gather/build/awaken/combat/death-cascade) — needs audio assets

**Deferred until combat + character pages ship:**
- **#38** City management (Era 4+) — villagers, role assignment, village-level threats

## Dev panel
Ctrl+Shift+D or floating 🛠️ (bottom-left). Gated by `import.meta.env.DEV` or `settings.devUnlocked`. All actions go through DEV_PATCH; force-fired threats pump flavor via `patch.events`.

Header status line: era · built · researched · alignment · echoes · WS · studies completed/in-progress · next-era requirements.

Six tabs:
- **🚀 Quick** — Era jumps (1/2/3) · full unlocks per era · rock + fragments · bulk resources
- **🌍 Content** — Per-item toggles for buildings/research/tools. ✓/×N status. Scroll/Ink recipes route to their resource ids.
- **🧠 State** — Per-stat sliders (0/50/max) for all seven stats · skill levels · alignment setters · spell-cooldown clear · status toggles (warded, dysentery 5min) · death-debuff controls (apply cascade, set magnitude, clear; live readout)
- **⚔️ Encounters** — Force-fire each threat by id (bypasses chance + warded gates) · equipment slots readout · pure weapons section (give + equip into either hand) · quick-equip dual-use tools (Axe/Knife/Pickaxe/Bow/FragKnife → right slot) · pest controls · event cooldown wipe · clear active event
- **🕯️ Arcane** — Stone Altar one-tap build (with prereqs) · scroll/ink grants · water-tier shortcuts · complete active study · complete all studies · study reset · World Score quick-set (0/5/15/30/50/80/100) + nudge ±5 · altar etchings inspector + wipe
- **⏱️ System** — Time skip · inventory dump · wipe run · nuke save

Test recipes:
- **Combat end-to-end**: Encounters → give every weapon → equip into handRight → Force Wild Dog → watch fight log
- **Combat-skill progression**: State → set swordplay to lvl 10 → equip melee weapon → Force Wild Dog → fight resolves with boosted acc/crit/damage. Lvl 20 swordplay = +10 flat damage per hit before crit double, +0.20 acc, +0.30 crit.
- **Boss fight (full flow)**: Quick → 🚀 Unlock all Era 1 → Encounters → give every weapon → equip into handRight → left rail **⚔️ Challenges** tab → "Open Challenges" → pick boss → Attack/Spell/Item/Defend/Flee → on victory, check inventory for `defeatReward`, log for `firstDefeatLog`, Arcane → altar etchings inspector for new stamp.
- **Rail-as-nav**: click left-rail icon → center view swaps (top group: 🌍/👤/🛠️; bottom group: Arcane/Studies) OR opens the Buildings tree modal. Hover any icon for the explainer tooltip.
- **Patrol view (#66/#67/#68/#69/#70)**: Left rail 🗡️ Patrol → mob cards grouped by era. **Click a card = auto-engage loop:** fight fires every `cycleMs`, card lights up with accent border + filling progress bar at the bottom, drops accrue into the per-target **Pile of Goods** above the era grid (chips with hover-tooltip resource cards). Click a different card → swaps target + resets pile. Click Stop → halts. **Hidden info reveal (#70):** mob HP/damage/accuracy/type AND drop names/qty/% are masked as ??? until `state.run.mobsDefeated[mobId]` hits per-field thresholds (HP: 1 kill, dropNames: 1, damage: 3, dropQty: 5, accuracy: 5, damageType: 10, dropChance: 10). Card shows next-reveal hint ("Beat 2 more to reveal damage"). **Butchering skill (#70):** new survival skill, earns XP per mob kill via `gainXp(run, "butchering", ⌊hp/6⌋)`; level scales drop chance (+1%/lvl, cap +0.20) and drop quantity (+5%/lvl additive multiplier, cap 2.0x at lvl 20) — read by `getButcheringBonuses(run)` inside `systems/patrol.js rollDrops`.
- **Auto-loop core (#68)**: `state.run.activeLoop = { kind, target, startedAt, cycleMs }` (single active loop). `systems/loop.js` exposes `setActiveLoop` / `clearActiveLoop` / `tickActiveLoop` / `getActiveLoop` / `getLoopProgress` / `computeCycleMs`. Reducer wires `SET_ACTIVE_LOOP` / `CLEAR_ACTIVE_LOOP` / `TICK_LOOP`. Store runs a 250ms `TICK_LOOP` interval (cheap when no loop active — reducer short-circuits). Phase 1 wires Patrol only; Phase 2 extends to Gather + Hunt + Ritual + crafting.
- **Character hub (#44 + #45 + #54)**: 👤 → jump-nav strip (📊/🎯/🛡️/🎒) on left edge. Stats: Survival (incl. STR at bottom) + Combat columns. Dev → State → Apply Death Debuff → STR drops in Survival. Skills section below stats — fight a foe → combat skill XP shows up in Combat-skills column. Encounters → give weapon → click → swap to Character page → Items tab (or scroll) → card shows weapon stats inline → "→ L" / "→ R" equips → slot fills above → click filled slot → returns to pack.
- **Right off-canvas**: click › inside right column → collapses to ‹ edge tab. Click tab → slides back as overlay over center (center stays wide). Persists via `lithos.rightPanelM